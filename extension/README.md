# Quadruzz Companion

1. Open `chrome://extensions`.
2. Enable **Developer mode**.
3. Choose **Load unpacked** and select this `extension` folder.
4. Open the extension, choose **Get pairing code**, and follow the pairing screen.

The popup reads server-authoritative membership and presence data. The website does not need to remain open after pairing.
